<Window x:Class="CibusMed.WPF.OrderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CibusMed.WPF"
        xmlns:domains="clr-namespace:CibusMed_App.Domains;assembly=CibusMed_App"
        mc:Ignorable="d"
        Title="Order Details" Height="600" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,20">
            <TextBlock Text="Order Details" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="{Binding Order.Id, StringFormat='Order #{0}'}" FontSize="14" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Order Information -->
                <Border BorderBrush="#CCCCCC" BorderThickness="1" Padding="15" Margin="0,0,0,15" Background="#F9F9F9">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Customer:" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Order.CustomerName}" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Order.CustomerEmail}" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Created Date:" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Order.CreatedAtUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Status:" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Order.Status}" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Notes:" FontWeight="Bold" VerticalAlignment="Top"/>
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Order.Notes}" TextWrapping="Wrap"/>
                    </Grid>
                </Border>

                <!-- Order Items -->
                <TextBlock Text="Order Items" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <Border BorderBrush="#CCCCCC" BorderThickness="1">
                    <DataGrid ItemsSource="{Binding Order.Items}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              RowHeight="30">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product SKU" Binding="{Binding ProductSku}" Width="120"/>
                            <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                            <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat='C2'}" Width="100"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat='C2'}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Total -->
                <Border BorderBrush="#CCCCCC" BorderThickness="1,0,1,1" Padding="10" Background="#F5F5F5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="Total Amount:" FontSize="16" FontWeight="Bold" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Order.TotalAmount, StringFormat='C2'}" FontSize="16" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Close" Width="80" Height="30" IsCancel="True"
                    Command="{Binding CloseCommand}"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="#80FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                Panel.ZIndex="1">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Loading..." FontSize="16" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
