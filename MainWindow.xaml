<Window x:Class="CibusMed.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CibusMed.WPF"
        xmlns:domains="clr-namespace:CibusMed_App.Domains;assembly=CibusMed_App"
        mc:Ignorable="d"
        Title="Order Management System" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Orders" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Filters Panel -->
            <Border Grid.Row="0" BorderBrush="#CCCCCC" BorderThickness="1" Padding="10" Margin="0,0,0,10" Background="#F5F5F5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Status Filter -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" Width="150" HorizontalAlignment="Left"
                              SelectedItem="{Binding SelectedStatus, Mode=TwoWay}">
                        <ComboBox.ItemsSource>
                            <x:Array Type="{x:Type domains:OrderStatus}">
                                <domains:OrderStatus>Pending</domains:OrderStatus>
                                <domains:OrderStatus>Approved</domains:OrderStatus>
                                <domains:OrderStatus>Shipped</domains:OrderStatus>
                                <domains:OrderStatus>Cancelled</domains:OrderStatus>
                            </x:Array>
                        </ComboBox.ItemsSource>
                    </ComboBox>

                    <!-- Search Box -->
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Search:" VerticalAlignment="Center" Margin="20,0,10,0"/>
                    <TextBox Grid.Row="0" Grid.Column="3" Width="200" HorizontalAlignment="Left"
                             Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <!-- Buttons -->
                    <StackPanel Grid.Row="0" Grid.Column="4" Orientation="Horizontal" Margin="20,0,0,0">
                        <Button Content="Refresh" Width="80" Height="30" Margin="0,0,10,0"
                                Command="{Binding LoadOrdersCommand}"/>
                        <Button Content="Clear Filters" Width="100" Height="30"
                                Command="{Binding ClearFiltersCommand}"/>
                    </StackPanel>

                    <!-- Date Filters -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="From Date:" VerticalAlignment="Center" Margin="0,10,10,0"/>
                    <DatePicker Grid.Row="1" Grid.Column="1" Width="150" HorizontalAlignment="Left" Margin="0,10,0,0"
                                SelectedDate="{Binding FromDate, Mode=TwoWay}"/>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="To Date:" VerticalAlignment="Center" Margin="20,10,10,0"/>
                    <DatePicker Grid.Row="1" Grid.Column="3" Width="150" HorizontalAlignment="Left" Margin="0,10,0,0"
                                SelectedDate="{Binding ToDate, Mode=TwoWay}"/>
                </Grid>
            </Border>

            <!-- Orders DataGrid -->
            <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1">
                <Grid>
                    <!-- Loading Overlay -->
                    <Border Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Panel.ZIndex="1">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="Loading..." FontSize="16" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid ItemsSource="{Binding Orders}"
                              SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              RowHeight="30"
                              MouseDoubleClick="DataGrid_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Order #" Binding="{Binding Id}" Width="80"/>
                            <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="150"/>
                            <DataGridTextColumn Header="Products" Binding="{Binding ProductNames}" Width="*"/>
                            <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedAtUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat='€#,##0.00'}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="0,1,0,0" Padding="5" Margin="0,10,0,0">
            <Grid>
                <TextBlock Text="{Binding Orders.Count, StringFormat='Total Orders: {0}'}" HorizontalAlignment="Left"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</Window>
